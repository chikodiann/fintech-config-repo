spring:
  application:
    name: account-service
  cloud:
    config:
      uri: http://localhost:2001
      username: admin
      password: configpass
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 5